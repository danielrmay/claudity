Run the /claudity:decide command for this decision: should the feature-flags CLI store flag state in SQLite or Postgres?

This is a non-interactive test run: do not ask the user any questions. Assume a small single-node deployment with one operator and low write volume. Work through the decision per the guide, choose SQLite, write the decision document and index entry, and record the decision state with the appropriate related documents. Then stop — do not continue to other processes.
