Read the failure-analysis process guide from the claudity plugin and follow it for this project. The brainstorming pool has pending raw failures. This is a non-interactive test run: do not ask the user any questions; where the guide offers the user choices, pick the most sensible option yourself and note it. Take the pool snapshot as the guide directs, group the raw failures into analyzed failure modes with chains, write the failure documents and the failures index, and record the document state per the guide — treat this run as the user having accepted the results. Then stop; do not continue to failure-management or any other process.
