I just realized something about the flags CLI that worries me: if the flags service is down when an engineer runs a flip command, the CLI queues it locally and retries silently — hours later the flag flips in production when nobody is watching, and on-call gets paged for a change no one remembers making. Use the clarity-agent record_failure tool to capture that as a risk (don't start a full brainstorming session), then tell me it's recorded.
